# Generated by 'cargo xtask gen-crds' — do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  name: autoscalinggroups.blc.samcday.com
spec:
  group: blc.samcday.com
  names:
    categories: []
    kind: AutoScalingGroup
    plural: autoscalinggroups
    shortNames:
    - asg
    singular: autoscalinggroup
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: '.spec.minSize'
      name: Min
      type: integer
    - jsonPath: '.spec.maxSize'
      name: Max
      type: integer
    - jsonPath: '.status.replicas'
      name: Replicas
      type: integer
    - jsonPath: '.status.lastScaleUp'
      name: Last Scale Up
      type: date
    - jsonPath: '.status.lastScaleDown'
      name: Last Scale Down
      type: date
    - jsonPath: '.spec.size'
      name: Size
      priority: 1
      type: string
    - jsonPath: '.spec.region'
      name: Region
      priority: 1
      type: string
    - jsonPath: '.metadata.creationTimestamp'
      name: Age
      priority: 1
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AutoScalingGroupSpec via `CustomResource`
        properties:
          spec:
            properties:
              diskGb:
                description: Override disk in GB (defaults from BL size catalog if omitted)
                format: int32
                nullable: true
                type: integer
              image:
                description: BinaryLane image slug
                type: string
              maxSize:
                format: int32
                type: integer
              memoryMb:
                description: Override memory in MB (defaults from BL size catalog if omitted)
                format: int32
                nullable: true
                type: integer
              minSize:
                format: int32
                type: integer
              namePrefix:
                description: Node name prefix. Nodes named {namePrefix}{asgName}-{ts}-{idx}
                type: string
              passwordSecretRef:
                description: Reference to a Secret containing a shared password for all nodes. If omitted, a password is auto-generated into {asgName}-password.
                nullable: true
                properties:
                  key:
                    description: Key within the Secret. Defaults to "user-data" or "password" depending on context.
                    nullable: true
                    type: string
                  name:
                    type: string
                  namespace:
                    description: Namespace of the Secret. Defaults to the controller's namespace.
                    nullable: true
                    type: string
                required:
                - name
                type: object
              region:
                description: BinaryLane region slug
                type: string
              size:
                description: BinaryLane size slug
                type: string
              userDataSecretRef:
                description: Reference to a Secret containing user-data for provisioned nodes
                nullable: true
                properties:
                  key:
                    description: Key within the Secret. Defaults to "user-data" or "password" depending on context.
                    nullable: true
                    type: string
                  name:
                    type: string
                  namespace:
                    description: Namespace of the Secret. Defaults to the controller's namespace.
                    nullable: true
                    type: string
                required:
                - name
                type: object
              vcpus:
                description: Override vCPU count (defaults from BL size catalog if omitted)
                format: int32
                nullable: true
                type: integer
            required:
            - image
            - maxSize
            - minSize
            - region
            - size
            type: object
          status:
            nullable: true
            properties:
              lastScaleDown:
                description: Timestamp of the last scale-down event
                format: date-time
                nullable: true
                type: string
              lastScaleUp:
                description: Timestamp of the last scale-up event
                format: date-time
                nullable: true
                type: string
              replicas:
                default: 0
                description: Current number of nodes in this group
                format: int32
                type: integer
            type: object
        required:
        - spec
        title: AutoScalingGroup
        type: object
    served: true
    storage: true
    subresources:
      status: {}
