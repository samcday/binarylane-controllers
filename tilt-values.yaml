# Override values for Tilt development.
# BL_API_TOKEN is set from environment in Tiltfile.
apiToken: "dev-token-replace-me"

autoscaler:
  enabled: false

mtls:
  enabled: false

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 256Mi
